logging:
  level:
    root: INFO

server:
  port: 8080

spring:
  application:
    name: lectures
  r2dbc:
    url: r2dbc:mariadb://localhost:3306/lecture
    username: root
    password: eddy
  data:
    redis:
      host: localhost
      port: 6379
